services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  dashboard:
    build: .
    command: poetry run dashboard
    env_file: .env
    depends_on:
      - redis
    stdin_open: true
    tty: true

volumes:
  redis_data:
