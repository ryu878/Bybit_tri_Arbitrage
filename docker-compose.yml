services:
  dashboard:
    build: .
    command: poetry run dashboard
    env_file: .env
    extra_hosts:
      - "host.docker.internal:host-gateway"   # reach host Redis from container (Linux)
    stdin_open: true
    tty: true
